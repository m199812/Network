import * as dntShim from "../_dnt.shims.js";
/** Use this function to close a ws connection properly */
export function closeWS(ws, code, reason) {
    if (ws.readyState !== dntShim.WebSocket.OPEN)
        return;
    ws.close(code, reason);
}
