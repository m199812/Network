/** Removes the bot from a thread. Requires the thread is not archived. */
export async function leaveThread(bot, threadId) {
    await bot.rest.runMethod(bot.rest, "delete", bot.constants.endpoints.THREAD_ME(threadId));
}
