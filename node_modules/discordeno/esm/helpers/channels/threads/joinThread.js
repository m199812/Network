/** Adds the bot to the thread. Cannot join an archived thread. */
export async function joinThread(bot, threadId) {
    await bot.rest.runMethod(bot.rest, "put", bot.constants.endpoints.THREAD_ME(threadId));
}
