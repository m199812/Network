/** Delete a scheduled event. */
export async function deleteScheduledEvent(bot, guildId, eventId) {
    await bot.rest.runMethod(bot.rest, "delete", bot.constants.endpoints.GUILD_SCHEDULED_EVENT(guildId, eventId));
}
