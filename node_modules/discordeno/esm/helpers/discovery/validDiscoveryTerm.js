export async function validDiscoveryTerm(bot, term) {
    const result = await bot.rest.runMethod(bot.rest, "get", `${bot.constants.endpoints.DISCOVERY_VALID_TERM()}?term=${term}`);
    return result.valid;
}
