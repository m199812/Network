/** Delete a guild role. Requires the MANAGE_ROLES permission. */
export async function deleteRole(bot, guildId, id) {
    await bot.rest.runMethod(bot.rest, "delete", bot.constants.endpoints.GUILD_ROLE(guildId, id));
}
