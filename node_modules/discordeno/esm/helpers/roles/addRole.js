/** Add a role to the member */
export async function addRole(bot, guildId, memberId, roleId, reason) {
    await bot.rest.runMethod(bot.rest, "put", bot.constants.endpoints.GUILD_MEMBER_ROLE(guildId, memberId, roleId), { reason });
}
