"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculateMaxShards = void 0;
/** Handler used to determine max number of shards to use based upon the max concurrency. */
function calculateMaxShards(maxShards, maxConcurrency) {
    if (maxShards < 100)
        return maxShards;
    return Math.ceil(maxShards /
        (maxConcurrency === 1 ? 16 : maxConcurrency)) * maxConcurrency;
}
exports.calculateMaxShards = calculateMaxShards;
