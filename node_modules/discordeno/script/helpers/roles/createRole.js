"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRole = void 0;
/** Create a new role for the guild. Requires the MANAGE_ROLES permission. */
async function createRole(bot, guildId, options, reason) {
    const result = await bot.rest.runMethod(bot.rest, "post", bot.constants.endpoints.GUILD_ROLES(guildId), {
        name: options.name,
        color: options.color,
        hoist: options.hoist,
        mentionable: options.mentionable,
        permissions: bot.utils.calculateBits(options?.permissions || []),
        reason,
    });
    return bot.transformers.role(bot, {
        role: result,
        guildId,
    });
}
exports.createRole = createRole;
