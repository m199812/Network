"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWidget = void 0;
/** Returns the widget for the guild. */
async function getWidget(bot, guildId) {
    const result = await bot.rest.runMethod(bot.rest, "get", `${bot.constants.endpoints.GUILD_WIDGET(guildId)}.json`);
    return bot.transformers.widget(bot, result);
}
exports.getWidget = getWidget;
