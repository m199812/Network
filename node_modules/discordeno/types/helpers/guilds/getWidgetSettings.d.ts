import type { Bot } from "../../bot.js";
/** Returns the guild widget object. Requires the MANAGE_GUILD permission. */
export declare function getWidgetSettings(bot: Bot, guildId: bigint): Promise<import("../../mod.js").GuildWidget>;
