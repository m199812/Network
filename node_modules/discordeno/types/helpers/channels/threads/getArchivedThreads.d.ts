import { Collection } from "../../../util/collection.js";
import type { Bot } from "../../../bot.js";
/** Get the archived threads for this channel, defaults to public */
export declare function getArchivedThreads(bot: Bot, channelId: bigint, options?: ListPublicArchivedThreads & {
    type?: "public" | "private" | "privateJoinedThreads";
}): Promise<{
    threads: Collection<bigint, import("../../../mod.js").Channel>;
    members: Collection<bigint | undefined, import("../../../mod.js").ThreadMember>;
}>;
export interface ListPublicArchivedThreads {
    /** Returns threads before this timestamp. UNIX or ISO8601 timestamp */
    before?: number | string;
    /** Optional maximum number of threads to return */
    limit?: number;
}
