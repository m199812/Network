import type { Bot } from "../../../bot.js";
import { ApplicationCommandPermissions } from "./batchEditApplicationCommandPermissions.js";
/** Edits command permissions for a specific command for your application in a guild. */
export declare function editApplicationCommandPermissions(bot: Bot, guildId: bigint, commandId: bigint, options: ApplicationCommandPermissions[]): Promise<import("../../../mod.js").ApplicationCommandPermission>;
