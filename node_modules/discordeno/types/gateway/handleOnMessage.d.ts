import { GatewayManager } from "./gatewayManager.js";
/** Handler for handling every message event from websocket. */
export declare function handleOnMessage(gateway: GatewayManager, message: any, shardId: number): Promise<void>;
