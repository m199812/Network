import { GatewayManager } from "./gatewayManager.js";
export declare function prepareBuckets(gateway: GatewayManager, firstShardId: number, lastShardId: number): void;
/** Begin spawning shards. */
export declare function spawnShards(gateway: GatewayManager, firstShardId?: number): void;
